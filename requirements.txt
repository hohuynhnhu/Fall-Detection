opencv-python
mediapipe==0.10.11
ultralytics
fastapi
uvicorn
numpy
httpx
pydantic>=2.0
pillow
torch
torchvision

# Audio detection (YAMNet) — optional; app runs without these
tensorflow>=2.13
tensorflow-hub>=0.15
sounddevice>=0.4

# Cloudinary + env
cloudinary>=1.36
python-dotenv>=1.0

# Face recognition — InsightFace buffalo_l (ArcFace, 512-d) + ONNX runtime
insightface
onnxruntime-gpu       # CUDA; swap for onnxruntime if no GPU
websockets
geocoder